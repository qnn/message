<% if user_signed_in? %>
  <li>
  <%= link_to(t("users.edit"), edit_user_registration_path) %>
  </li>
<% else %>
  <li>
  <%= link_to(t("users.register"), new_user_registration_path)  %>
  </li>
<% end %>
